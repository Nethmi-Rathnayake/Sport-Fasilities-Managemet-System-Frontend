# Base URL of the Laravel backend (php artisan serve defaults to port 8000).
# CRA only exposes env vars prefixed with REACT_APP_. Restart `npm start` after changing this.
REACT_APP_API_URL=http://localhost:8000


